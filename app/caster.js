"use strict";
var Caster = (function () {
    function Caster(host, port, mountPt, user, password) {
        this.host = host;
        this.port = port;
        this.mountPt = mountPt;
        this.user = user;
        this.password = password;
    }
    return Caster;
}());
exports.Caster = Caster;
//# sourceMappingURL=caster.js.map